automation:
  - id: 'morning_briefing_telegram'
    alias: 'التقرير الصباحي - تيليغرام'
    description: 'إرسال تقرير صباحي يومي عبر تيليغرام في الساعة 7:00 صباحاً'
    triggers:
      - trigger: time
        at: '07:00:00'

    actions:
      - action: telegram_bot.send_message
        data:
          parse_mode: html
          message: >-
            <b>🌅 التقرير الصباحي</b>

            <b>🪣 خزان الماء:</b>
            {% set water = states('sensor.water_tank_level') %}
            {% if water not in ['unknown','unavailable','none',''] %}
            {{ water | float(0) | round(1) }}%
            {% else %}
            غير متوفر
            {% endif %}

            <b>🔋 بطارية المنزل:</b>
            {% set batt = states('sensor.home_battery_level_percent') %}
            {% if batt not in ['unknown','unavailable','none',''] %}
            {{ batt | float(0) | round(1) }}%
            {% else %}
            غير متوفر
            {% endif %}

            <b>⚡ الكهرباء خلال آخر 24 ساعة:</b>
            متوفرة {{ states('sensor.power_grid_on_hours_24h') | float(0) | round(1) }} س
            / مقطوعة {{ states('sensor.power_grid_off_hours_24h') | float(0) | round(1) }} س

            {% set yesterday_kwh = states('sensor.power_grid_energy_yesterday') %}
            {% set avg_kwh = states('sensor.power_grid_energy_weekly_average') %}

            <b>📊 استهلاك الأمس:</b>
            {% if yesterday_kwh not in ['unknown','unavailable','none',''] %}
            {{ yesterday_kwh | float(0) | round(2) }} ك.و.س
            {% else %}
            غير متوفر
            {% endif %}

            {% if avg_kwh not in ['unknown','unavailable','none',''] and yesterday_kwh not in ['unknown','unavailable','none',''] %}
            {% set avg = avg_kwh | float(0) %}
            {% set diff = (yesterday_kwh | float(0) - avg) | round(2) %}

            {% if avg > 0 %}
            <b>📈 معدل 7 أيام:</b>
            {{ avg | round(2) }} ك.و.س

            <b>🔎 مقارنة بالمعدل:</b>
            {% if diff > 0 %}
            ⬆️ أعلى من المعدل بـ {{ diff }} ك.و.س
            {% elif diff < 0 %}
            ⬇️ أقل من المعدل بـ {{ (diff * -1) | round(2) }} ك.و.س
            {% else %}
            ➡️ مساوي للمعدل
            {% endif %}
            {% endif %}
            {% endif %}

            <b>🗓️ استهلاك الدورة الحالية:</b>
            {% set cycle = states('sensor.power_grid_energy_current_cycle') %}
            {% if cycle not in ['unknown','unavailable','none',''] %}
            {{ cycle | float(0) | round(2) }} ك.و.س
            {% else %}
            غير متوفر
            {% endif %}

            <b>💵 فاتورة الدورة الحالية:</b>
            {% set bill = states('sensor.power_grid_current_cycle_bill') %}
            {% if bill not in ['unknown','unavailable','none',''] %}
            {{ bill | float(0) | round(0) }} ل.س
            {% else %}
            غير متوفر
            {% endif %}

            <b>📅 الدورة الحالية:</b>
            {{ states('sensor.power_grid_current_billing_cycle') }}

    mode: single