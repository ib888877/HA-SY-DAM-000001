type: sections
max_columns: 4
icon: mdi:lightning-bolt
path: energy
title: الطاقة

sections:
  - type: grid
    title: ملخص الاستهلاك
    cards:
      - type: custom:button-card
        entity: sensor.power_grid_energy_today
        name: استهلاك اليوم
        icon: mdi:calendar-today
        show_state: false
        tap_action:
          action: more-info
        hold_action:
          action: more-info
        custom_fields:
          value: >
            [[[
              const v = Number(entity.state);
              return Number.isFinite(v) ? `${v.toFixed(2)} kWh` : '--';
            ]]]
          sub: >
            [[[
              const bill = states['sensor.power_grid_daily_bill_estimate']?.state;
              const v = Number(bill);
              return Number.isFinite(v) ? `تقديري: ${Math.round(v).toLocaleString()} ل.س` : 'تقديري: --';
            ]]]
        styles:
          card:
            - border-radius: 20px
            - padding: 16px
            - height: 108px
            - box-shadow: 0 6px 14px rgba(0,0,0,0.20)
            - background: linear-gradient(135deg, #2f7a6d 0%, #68b0a1 100%)
          grid:
            - grid-template-areas: '"i n" "i value" "i sub"'
            - grid-template-columns: 52px 1fr
            - grid-template-rows: min-content min-content 1fr
            - column-gap: 12px
          img_cell:
            - align-self: center
            - justify-self: center
            - width: 36px
            - height: 36px
          icon:
            - color: white
            - width: 34px
          name:
            - align-self: end
            - justify-self: start
            - color: white
            - font-size: 16px
            - font-weight: 700
          custom_fields:
            value:
              - align-self: center
              - justify-self: start
              - color: white
              - font-size: 22px
              - font-weight: 800
              - line-height: 1.2
            sub:
              - align-self: start
              - justify-self: start
              - color: rgba(255,255,255,0.92)
              - font-size: 14px
              - font-weight: 700

      - type: custom:button-card
        entity: sensor.power_grid_energy_yesterday
        name: استهلاك الأمس
        icon: mdi:calendar-today
        show_state: false
        tap_action:
          action: more-info
        hold_action:
          action: more-info
        custom_fields:
          value: >
            [[[
              const v = Number(entity.state);
              return Number.isFinite(v) ? `${v.toFixed(2)} kWh` : '--';
            ]]]
          sub: >
            [[[
              const avg = states['sensor.power_grid_energy_weekly_average']?.state;
              const v = Number(avg);
              return Number.isFinite(v) ? `معدل 7 أيام: ${v.toFixed(2)} kWh` : 'معدل 7 أيام: --';
            ]]]
        styles:
          card:
            - border-radius: 20px
            - padding: 16px
            - height: 108px
            - box-shadow: 0 6px 14px rgba(0,0,0,0.20)
            - background: linear-gradient(135deg, #455a64 0%, #78909c 100%)
          grid:
            - grid-template-areas: '"i n" "i value" "i sub"'
            - grid-template-columns: 52px 1fr
            - grid-template-rows: min-content min-content 1fr
            - column-gap: 12px
          img_cell:
            - align-self: center
            - justify-self: center
            - width: 36px
            - height: 36px
          icon:
            - color: white
            - width: 34px
          name:
            - align-self: end
            - justify-self: start
            - color: white
            - font-size: 16px
            - font-weight: 700
          custom_fields:
            value:
              - align-self: center
              - justify-self: start
              - color: white
              - font-size: 22px
              - font-weight: 800
              - line-height: 1.2
            sub:
              - align-self: start
              - justify-self: start
              - color: rgba(255,255,255,0.92)
              - font-size: 14px
              - font-weight: 700

      - type: custom:button-card
        entity: sensor.power_grid_energy_current_cycle
        name: استهلاك الدورة الحالية
        icon: mdi:lightning-bolt-circle
        show_state: false
        tap_action:
          action: more-info
        hold_action:
          action: more-info
        custom_fields:
          value: >
            [[[
              const v = Number(entity.state);
              return Number.isFinite(v) ? `${v.toFixed(2)} kWh` : '--';
            ]]]
          sub: >
            [[[
              return states['sensor.power_grid_current_billing_cycle']?.state || '--';
            ]]]
        styles:
          card:
            - border-radius: 20px
            - padding: 16px
            - height: 108px
            - box-shadow: 0 6px 14px rgba(0,0,0,0.20)
            - background: linear-gradient(135deg, #7da453 0%, #a8c66c 100%)
          grid:
            - grid-template-areas: '"i n" "i value" "i sub"'
            - grid-template-columns: 52px 1fr
            - grid-template-rows: min-content min-content 1fr
            - column-gap: 12px
          img_cell:
            - align-self: center
            - justify-self: center
            - width: 36px
            - height: 36px
          icon:
            - color: white
            - width: 34px
          name:
            - align-self: end
            - justify-self: start
            - color: white
            - font-size: 16px
            - font-weight: 700
          custom_fields:
            value:
              - align-self: center
              - justify-self: start
              - color: white
              - font-size: 22px
              - font-weight: 800
              - line-height: 1.2
            sub:
              - align-self: start
              - justify-self: start
              - color: rgba(255,255,255,0.92)
              - font-size: 14px
              - font-weight: 700

      - type: custom:button-card
        entity: sensor.power_grid_current_cycle_bill
        name: فاتورة الدورة الحالية
        icon: mdi:cash-multiple
        show_state: false
        tap_action:
          action: more-info
        hold_action:
          action: more-info
        custom_fields:
          value: >
            [[[
              const v = Number(entity.state);
              return Number.isFinite(v) ? `${Math.round(v).toLocaleString()} ل.س` : '--';
            ]]]
          sub: >
            [[[
              return states['sensor.power_grid_bill_tier_current_cycle']?.state || '--';
            ]]]
        styles:
          card:
            - border-radius: 20px
            - padding: 16px
            - height: 108px
            - box-shadow: 0 6px 14px rgba(0,0,0,0.20)
            - background: linear-gradient(135deg, #c07c2c 0%, #e0a85b 100%)
          grid:
            - grid-template-areas: '"i n" "i value" "i sub"'
            - grid-template-columns: 52px 1fr
            - grid-template-rows: min-content min-content 1fr
            - column-gap: 12px
          img_cell:
            - align-self: center
            - justify-self: center
            - width: 36px
            - height: 36px
          icon:
            - color: white
            - width: 34px
          name:
            - align-self: end
            - justify-self: start
            - color: white
            - font-size: 16px
            - font-weight: 700
          custom_fields:
            value:
              - align-self: center
              - justify-self: start
              - color: white
              - font-size: 22px
              - font-weight: 800
              - line-height: 1.2
            sub:
              - align-self: start
              - justify-self: start
              - color: rgba(255,255,255,0.92)
              - font-size: 13px
              - font-weight: 700

      - type: custom:button-card
        entity: sensor.power_grid_energy_last_cycle
        name: استهلاك الدورة الماضية
        icon: mdi:calendar-arrow-left
        show_state: false
        tap_action:
          action: more-info
        hold_action:
          action: more-info
        custom_fields:
          value: >
            [[[
              const v = Number(entity.state);
              return Number.isFinite(v) ? `${v.toFixed(2)} kWh` : '--';
            ]]]
          sub: >
            [[[
              const bill = states['sensor.power_grid_last_cycle_bill']?.state;
              const v = Number(bill);
              return Number.isFinite(v) ? `فاتورة: ${Math.round(v).toLocaleString()} ل.س` : 'فاتورة: --';
            ]]]
        styles:
          card:
            - border-radius: 20px
            - padding: 16px
            - height: 108px
            - box-shadow: 0 6px 14px rgba(0,0,0,0.20)
            - background: linear-gradient(135deg, #8d6e63 0%, #bcaaa4 100%)
          grid:
            - grid-template-areas: '"i n" "i value" "i sub"'
            - grid-template-columns: 52px 1fr
            - grid-template-rows: min-content min-content 1fr
            - column-gap: 12px
          img_cell:
            - align-self: center
            - justify-self: center
            - width: 36px
            - height: 36px
          icon:
            - color: white
            - width: 34px
          name:
            - align-self: end
            - justify-self: start
            - color: white
            - font-size: 16px
            - font-weight: 700
          custom_fields:
            value:
              - align-self: center
              - justify-self: start
              - color: white
              - font-size: 22px
              - font-weight: 800
              - line-height: 1.2
            sub:
              - align-self: start
              - justify-self: start
              - color: rgba(255,255,255,0.92)
              - font-size: 14px
              - font-weight: 700

      - type: custom:button-card
        entity: sensor.power_grid_last_cycle_bill
        name: فاتورة الدورة الماضية
        icon: mdi:cash-check
        show_state: false
        tap_action:
          action: more-info
        hold_action:
          action: more-info
        custom_fields:
          value: >
            [[[
              const v = Number(entity.state);
              return Number.isFinite(v) ? `${Math.round(v).toLocaleString()} ل.س` : '--';
            ]]]
          sub: >
            [[[
              const kwh = states['sensor.power_grid_energy_last_cycle']?.state;
              const v = Number(kwh);
              return Number.isFinite(v) ? `استهلاك: ${v.toFixed(2)} kWh` : 'استهلاك: --';
            ]]]
        styles:
          card:
            - border-radius: 20px
            - padding: 16px
            - height: 108px
            - box-shadow: 0 6px 14px rgba(0,0,0,0.20)
            - background: linear-gradient(135deg, #5e548e 0%, #9d8ec7 100%)
          grid:
            - grid-template-areas: '"i n" "i value" "i sub"'
            - grid-template-columns: 52px 1fr
            - grid-template-rows: min-content min-content 1fr
            - column-gap: 12px
          img_cell:
            - align-self: center
            - justify-self: center
            - width: 36px
            - height: 36px
          icon:
            - color: white
            - width: 34px
          name:
            - align-self: end
            - justify-self: start
            - color: white
            - font-size: 16px
            - font-weight: 700
          custom_fields:
            value:
              - align-self: center
              - justify-self: start
              - color: white
              - font-size: 22px
              - font-weight: 800
              - line-height: 1.2
            sub:
              - align-self: start
              - justify-self: start
              - color: rgba(255,255,255,0.92)
              - font-size: 13px
              - font-weight: 700

      - type: custom:button-card
        entity: sensor.power_grid_current_billing_cycle
        name: اسم الدورة الحالية
        icon: mdi:calendar-range
        show_state: false
        tap_action:
          action: more-info
        hold_action:
          action: more-info
        custom_fields:
          value: >
            [[[
              return entity.state || '--';
            ]]]
          sub: >
            [[[
              const kwh = states['sensor.power_grid_energy_current_cycle']?.state;
              const v = Number(kwh);
              return Number.isFinite(v) ? `الاستهلاك: ${v.toFixed(2)} kWh` : 'الاستهلاك: --';
            ]]]
        styles:
          card:
            - border-radius: 20px
            - padding: 16px
            - height: 108px
            - box-shadow: 0 6px 14px rgba(0,0,0,0.20)
            - background: linear-gradient(135deg, #0f4c81 0%, #4d7ea8 100%)
          grid:
            - grid-template-areas: '"i n" "i value" "i sub"'
            - grid-template-columns: 52px 1fr
            - grid-template-rows: min-content min-content 1fr
            - column-gap: 12px
          img_cell:
            - align-self: center
            - justify-self: center
            - width: 36px
            - height: 36px
          icon:
            - color: white
            - width: 34px
          name:
            - align-self: end
            - justify-self: start
            - color: white
            - font-size: 16px
            - font-weight: 700
          custom_fields:
            value:
              - align-self: center
              - justify-self: start
              - color: white
              - font-size: 18px
              - font-weight: 800
              - line-height: 1.2
            sub:
              - align-self: start
              - justify-self: start
              - color: rgba(255,255,255,0.92)
              - font-size: 13px
              - font-weight: 700

      - type: custom:button-card
        entity: sensor.power_grid_bill_tier_current_cycle
        name: الشريحة الحالية
        show_state: false
        icon: mdi:scale-balance
        tap_action:
          action: more-info
        hold_action:
          action: more-info
        custom_fields:
          value: >
            [[[
              return entity.state || '--';
            ]]]
          sub: >
            [[[
              const bill = states['sensor.power_grid_current_cycle_bill']?.state;
              const v = Number(bill);
              return Number.isFinite(v) ? `الفاتورة: ${Math.round(v).toLocaleString()} ل.س` : 'الفاتورة: --';
            ]]]
        styles:
          card:
            - border-radius: 20px
            - padding: 16px
            - height: 108px
            - box-shadow: 0 6px 14px rgba(0,0,0,0.20)
            - background: linear-gradient(135deg, #7b1e3a 0%, #b24c63 100%)
          grid:
            - grid-template-areas: '"i n" "i value" "i sub"'
            - grid-template-columns: 52px 1fr
            - grid-template-rows: min-content min-content 1fr
            - column-gap: 12px
          img_cell:
            - align-self: center
            - justify-self: center
            - width: 36px
            - height: 36px
          icon:
            - color: white
            - width: 34px
          name:
            - align-self: end
            - justify-self: start
            - color: white
            - font-size: 16px
            - font-weight: 700
          custom_fields:
            value:
              - align-self: center
              - justify-self: start
              - color: white
              - font-size: 16px
              - font-weight: 800
              - line-height: 1.2
            sub:
              - align-self: start
              - justify-self: start
              - color: rgba(255,255,255,0.92)
              - font-size: 13px
              - font-weight: 700

  - type: grid
    title: الاستهلاك اللحظي
    cards:
      - type: sensor
        entity: sensor.power_grid_power_display
        name: الكهرباء
        graph: line
        detail: 2
        hours_to_show: 24
        unit: W